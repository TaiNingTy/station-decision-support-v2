# V2 面试 Demo：预运行 AI 管线、静态交付、可选实时解释

更新：2026-09-14。状态：已按用户要求调整架构与验收范围；本轮未搭建应用、未部署、未改线上 GitHub 或 Squarespace。

角色修订：Coze作为准备阶段的AI工作流引擎，实际执行知识检索、场地解读、候选方案比较与风险分析，保存原始输出和引用。可选的是现场实时调用，而不是准备阶段是否真正运行AI。三城V2功能范围保留；当前尚未实现全部功能，也尚未在新三城输入上运行Coze。

## 1. 已确定的优先级

目标是面试期间稳定展示和清楚讲解，允许离线预处理、预计算和预制方案。默认流程不依赖 Coze、实时 GIS/OD 接口、数据库或服务端计算。

这里“离线/预运行”指在面试之前执行，不表示云端Coze能断网本地运行；准备阶段调用云端仍需要网络与可用工作流权限。面试运行时读取已经完成并审校的产物。

| 能力 | 默认实现 | 是否需要运行时后端 |
|---|---|---|
| GIS 清洗、投影、实体站映射、空间关联 | 离线处理，保存版本化的轻量矢量数据与转换记录 | 否 |
| 需求情景、规则校验、泊位/尺寸/布局 | 离线运行可复算引擎，结果作为静态 JSON | 否 |
| 知识检索、场地解读、风险与方案比较 | 准备阶段实际运行Coze agents与KB，保存模型/工作流版本、输入、检索命中与输出 | 准备阶段需要AI工作流；面试运行时不需要 |
| 地图、站点卡、时间轴、参数图 | 浏览器读取打包资源并渲染 | 否 |
| 解释与溯源 | 随方案打包的已审校说明、规则引用与来源摘要 | 否 |
| 有限参数的确定性重算 | 浏览器运行共享的纯 JS/TS 计算模块；必要时 Web Worker | 否 |
| Coze 实时解释 | 用户主动触发；通过独立代理调用 | 是，仅此层 |
| 运行结果保存 | 预制结果进仓库；用户探索结果可本地导出 | 无需数据库 |

计算在浏览器里并不会自动让人看懂“确定性”；界面需展示输入、公式、规则版本和输出，并能复算出一致结果。

### 准备阶段的正确顺序

数据标准化与空间处理 → Coze检索相关规范/案例并提出适用性与数据缺口 → 复核适用规则与项目目标 → 确定性校验、计算和候选方案生成 → Coze依据已计算方案解释差异、形成风险和建议 → 人工审校 → 合并为静态recommendations.json及配套地图/图形。

规则引擎执行的是版本化、已确认适用的规则；不能让RAG临时检索的片段直接替换硬阈值，也不能在计算完成后事后寻找一句话来证明原数字正确。数据测量、公式与参数自身有来源，完整溯源由程序将它们与AI引用连接起来，并非全部由LLM生成。

集中/分散的可容纳性、成本、排队和服务结果来自计算；LLM解释比较、补充风险与提出建议。“建议暂缓”依据项目目标、证据和方案评价，由人最终决定，不能仅由无约束语言判断决定投资。

自动批量调用仍需核实Coze的工作流发布、鉴权及导出能力。若暂未具备API，可先在Coze界面运行并整理代表性场景的真实输出；手动执行要如实标记，不能称作已完成自动批处理。Coze可替换需要稳定输入输出与检索接口，并非任意更换LLM就能零改动迁移。

## 2. 网站与部署

- addon.work 保留 Squarespace 7.1，承担项目叙事、个人职责、成果、视频和 Demo 入口。
- 新增交互应用继续采用 Next.js/React，但按静态导出设计；产物为 HTML/CSS/JS/JSON/SVG 等静态资源。
- 首选 GitHub Pages 作为静态发布方式，另保留同版本的本地演示构建；以后可配置子域名。
- 可选 Coze 代理独立部署，Vercel 只是可选承载方式，不是静态 Demo 的前置账号或平台要求。不开启代理也必须能完成全部面试主流程。
- 不在静态导出的主应用中设置需要请求时执行的 API 路由；独立代理与主应用通过可选接口连接。
- 源码、数据清单、规则、生成脚本、预制结果和发布说明可放在同一产品仓库。后续由 GitHub Actions 构建并发布静态产物，不依赖 Vercel 才能形成可追溯的仓库—产品关系。

Next.js 官方说明静态导出可部署到提供 HTML/CSS/JS 的静态服务器；请求时动态 Route Handlers、Server Actions 等不属于该模式支持的功能。[Next.js 静态导出](https://nextjs.org/docs/app/guides/static-exports)

GitHub Pages 托管仓库中的静态 HTML/CSS/JS，支持项目站点与自定义域名。[GitHub Pages](https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages)

构建需处理 GitHub 项目路径前缀、静态资源路径与直接刷新。不能把“浏览器双击任意 HTML 文件”当作所有模块/JSON加载必然可用的离线方案；本地演示使用预构建产物和准备好的本地静态服务器，现场不下载依赖。

## 3. 三种状态明确展示

### 预计算方案：默认面试模式

先提供少量经过完整核对的情景，例如基准、需求高峰和空间受限方案。情景切换时，地图、需求、泊位、尺寸、图形、规则结果、引用和解释作为一个整体切换。

界面用简短标签“预计算方案”，过程按钮可以叫“查看分析过程”。可以逐步展示源数据、JSON、规则和配置，但不能把已有轨迹标成正在发生的 Coze 调用。

预制不等于手填任意结果：必须由版本化输入和计算生成，并保留计算记录。真实数据、情景参数和工程待核项继续区分，不把公开假设改称企业真实性能。

### 浏览器重算：可选的交互探索

允许调整已经实现且经过验证的参数，例如服务周期或特定需求情景。重算只承诺实际支持的计算范围；如果只重算单站初步容量，不得同步声称全网队列和瓶颈也完成了更新。

规则与计算采用离线预计算共用的纯函数，输入与版本相同则输出一致。每次修改参数后，关联结果和解释须更新或标记待重新计算；不保留与当前参数不一致的旧尺寸/图形/说明。

如某些参数组合只有预计算方案可用，控件采用离散选项，不提供无实际计算支撑的连续滑块或插值结论。

### 实时解释：可选增强

按钮可叫“获取实时 AI 解释”。代理接收当前方案的结构化摘要、适用规则和证据，由 Coze 组织说明；数值和合规判断不由 LLM 改写。

结果记录请求对应的 scenario_id/input_hash，过期响应不覆盖当前方案。超时或失败时，主展示继续工作，实时说明标记未完成；已有同方案预制解释仍然可读。未配置 API 时不发起调用，也不显示伪造成功状态。

代理至少限制请求大小、调用频率、超时与允许来源，密钥仅服务端保存；来源限制不应被当作完整鉴权。首版可以完全不部署该层。

## 4. 静态资产也要能独立运行

仅把数值存成 JSON 还不够。主流程还应包括：

- 打包本站点、线路、场地和必要背景的轻量 GeoJSON/矢量图；采用允许分发的数据与素材，并保留署名。
- 不依赖外部地图瓦片、在线 geocoding、实时交通或 CDN 才能显示核心地图。卫星影像和外部底图可作为可选图层。
- 字体、图标、关键图片、样式和 JS 随构建提供；外链文献之外，打包足够解释当前方案的来源摘要、条款信息与处理说明。
- 重要参数图由当前方案数据生成或加载与该方案绑定的预渲染 SVG，不只替换数字标签。
- 保存本地静态构建和关键演示视频作为备份。首次打开在线站点本身仍需网络；现场离线保障来自事先准备并验证的本地版本。

不批量下载无授权的在线地图瓦片。许可尚未确定的数据或素材不会因“需要离线”就自动获得分发权。

## 5. 每个预制方案的最小记录

- scenario_id、标题、场景说明与适用范围。
- data_version、engine_version、rules_version、input_hash、生成时间。
- 原始/标准化输入、单位、来源、真实与情景字段状态。
- 计算中间结果、逐条规则结果、逐站配置与实际已计算的全网结果。
- 地图对象ID、站台图形参数、总占地与净尺寸。
- 规则/知识库引用、来源摘要及已审校解释。
- 实际AI运行记录：workflow/model版本、输入、检索命中及文档版本、输出、执行方式与复核状态；只有实际返回运行ID时才记录为该平台run_id。
- 结果生成方式：预计算或浏览器；解释方式：预制或Coze实时。

GIS重处理不在面试点击时运行，其预处理过程和结果应在资料栏可查。KB也不必在现场重新检索：可保存实际离线检索轨迹；人工选定的引用则标为已整理依据，不能伪装成检索命中。

## 6. 既有 JS 的迁移边界

rule-check/data-fusion 可成为离线与浏览器共用核心的起点，但不能未经修正直接上线：先处理工业用地一刀切、收入缺失默认1、未知曲线变false、单位/边界、字段映射和规则来源冲突。配置优化模块仍需真正实现。

模块需要适配浏览器打包，不能仅把使用 CommonJS/Node 环境的文件复制进 script 标签。UI与计算分开，计算模块不依赖DOM、网络或当前时间等隐藏输入；需要随机仿真时显式固定并记录随机种子。

## 7. 最小验收

1. 未配置任何 Coze API 凭证，主流程依然完整。
   面试运行时无需凭证；准备阶段应已完成真实AI执行、保存原始记录并审校。尚未完成的部分标为未完成，不以人工文字替代后宣称已运行AI。
2. 本地构建在断网条件下可加载、切换全部准备情景、查看站点与溯源、显示图形。
3. 每个准备情景都有输入—输出一致性核对；浏览器重算与相同版本的离线计算一致。
4. 模拟Coze失败/超时不影响主流程；旧请求不能覆盖切换后的方案。
5. 修改参数后不展示过期数字、图形或解释；不支持的组合不能假装已完成全网计算。
6. GitHub Pages路径、刷新、地图/字体/图标加载和主要演示屏幕尺寸经过实际检查。
7. 主讲场景有固定顺序和一键恢复基准功能；参数探索不会破坏准备好的情景。

这些是待实现和执行的验收条件，不是目前已通过的测试或“100%不会失败”的保证。

## 8. 对 A–F 和三城的影响

- A/B/D：继续准备真实字段、版本化规则与依据，但现场不依赖在线获取。
- C：先离线生成与验证全部主讲情景，再提供有限浏览器重算。
- E：增加断网、API缺失、外部资源失败、场景一致性和预计算/浏览器结果对照。
- F：默认完整预计算交互展示，live解释为附加按钮；精细3D与动画继续后置。
- 先Miami形成一个可完整讲解的产品，再迁移Melbourne机场分散站点和Singapore现状分析逻辑；数据与规则差异继续保留。

关联：[A–F升级清单](V2_AF对照与升级实施清单.md)、[三城设计](V2_三城案例与可视化设计底稿.md)。本文件作为最新架构决定，取代此前把运行时服务端计算和实时AI调用作为主流程的建议，并明确AI在准备阶段实际运行的要求。
