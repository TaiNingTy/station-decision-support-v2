# V2 站点输入包字段契约（Miami，第 0 步草案）

第二稿，2026-09-15。目的：在编写人口、就业、POI、步行路网各层脚本之前，先固定指标、分子分母、年份、地理版本、单位与缺失状态。规则依据见 [V2 数据语义与客流转换规则](V2_数据语义与客流转换规则.md)。

核对方法：表格代码、标签与宇宙由脚本直接解析 Census API 2024 年 5 年期各表的原始 JSON，逐字打印核对；LODES 的文件定义、`C000` 定义与版本从 `LODESTechDoc8.4.pdf` 原文抽取核对。第一稿的 B08301 与 B25044 代码曾经由摘要转述而出错，经独立评测指出后按原始 JSON 更正。

## 1. 版本锁定

| 组件 | 锁定值 | 说明 |
|---|---|---|
| ACS | 2020–2024 5 年期，vintage 2024，API 端点 `2024/acs/acs5` | 期间估计，不是当前人口 |
| ACS 地理 | block group，TIGER/Line 2024（2020 普查划分），Miami-Dade 县 FIPS 12086 | 取全县完整多边形，不按窗口裁切 |
| 陆地几何 | 同版本 TIGER/Line 县级水域面要素 AREAWATER，从 block 多边形中擦除水域 | 与 block 多边形属同一 TIGER/Line 产品，不是新的统计来源 |
| LODES | 第 8 版，技术文档 `LODESTechDoc8.4.pdf`（2025-12-03），佛州 2023 年 | 行政数据建模，无抽样误差 |
| LODES 地理 | 2020 普查 block，TIGER/Line 2020 `tabblock20`，`GEOID20` 15 位 | block group GEOID 是 block GEOID 的前 12 位，可按需聚合 |
| LODES 文件 | `fl_wac_S000_JT00_2023`、`fl_wac_S000_JT01_2023`、`fl_rac_S000_JT00_2023`、`fl_rac_S000_JT01_2023`、`fl_od_main_JT00_2023`、`fl_od_main_JT01_2023`、`fl_od_aux_JT00_2023`、`fl_od_aux_JT01_2023`，另取官方校验文件 `lodes_fl.sha256sum` 与 `version.txt` | 记录官方 sha256、本地 sha256 与下载日期 |
| LODES 覆盖 | 技术文档 8.4 原文：main 含居住地与工作地都在本州的岗位；aux 含工作地在本州、居住地在州外的岗位 | **佛州居民在州外工作的联系在其他州的 aux 文件里，本清单不取**，作为已声明缺口；对下城走廊影响很小，但不得声称完整覆盖 |
| 站点与环带 | 站点主表与空间连接当前包（`spatial_join_current.json` 解析后的包 ID） | 输入包锁定包 ID，不锁"当前指针" |
| 面积单位 | 内部 m²；展示 acre = 4,046.856 m²，sq mi = 2,589,988.11 m² | 与空间连接阶段一致 |

跨统计期指标必须标 `mixed_period: true` 并写出两端统计期。

## 2. 数值信封：每个指标值携带的字段

指标级元数据（同一指标对所有站相同）：`indicator_id`、`name`、`definition`、`numerator`、`denominator`、`source_table`、`source_variables`、`universe`、`geography_unit`、`period`、`vintage`、`unit_internal`、`unit_display`、`aggregation_rule`（sum / ratio / bracket_median / not_aggregatable）、`moe_rule`（rss / weighted_rss / ratio_formula / not_applicable / sensitivity_range）、`provenance_class`、`data_nature`、`weight_proxy`、`depends_on`、`notes`。

数值级字段（每站、每环带、每指标）：

| 字段 | 取值 | 说明 |
|---|---|---|
| `value` | 数值或 null | 内部单位 |
| `display` | 英制展示值 | 舍入规则同空间连接阶段 |
| `moe_90` | 数值或 null | 90% 置信区间误差幅度 |
| `moe_status` | `exact_source` / `approximate_sampling_only_excludes_interpolation` / `ratio_approximation` / `not_applicable` / `insufficient_sample` / `open_ended_interval` | ACS 汇总值一律是近似抽样误差 |
| `cv` | 数值或 null | `(moe_90 ÷ 1.645) ÷ value`；value 为 0 时 null |
| `reliability_flag` | `high` / `medium` / `low` / `undefined` | 阈值：cv ≤ 0.12 高，0.12–0.40 中，> 0.40 低；这是通行惯例，不是 Census 规定 |
| `value_status` | `value` / `missing` / `suppressed` / `not_applicable` / `confirmed_zero` / `zero_absent_from_file` | 数值状态，见第 7 节；获取状态是另一个字段 `acquisition_status`，见第 8 节 |
| `missing_reason` | `insufficient_sample` / `not_published` / `not_acquired` / null | 仅当 `value_status` 为 `missing` 或 `suppressed` 时填写 |
| `provenance_class` | `observed` / `derived` / `assumption` | |
| `data_nature` | `measured` / `survey_estimate` / `administrative_record`（行政记录，如分区图层、GTFS 时刻表）/ `administrative_modeled` / `design_specification`（设计图纸给出的规格值，如站台模块尺寸）/ `model_output` / `assumption` | |
| `sensitivity_range` | `[low, high]` 或 null | 只用于分组中位数等无公式误差的推算值，不是置信区间 |
| `interpolation` | `{method, weight_proxy, source_units: [{geoid, share}]}` | 分摊到环带时记录每个来源单元的份额 |
| `evidence_ids` | 列表 | 指向逐单元证据行 |
| `depends_on` | 列表 | 派生值所依赖的指标 ID 与假设 ID |

## 3. 人口层指标（ACS 5 年期，block group）

所有计数在 block group 层取值，再按第 6 节分摊到环带。比例类指标在环带层由汇总后的分子分母计算。

| ID | 指标 | 表 / 变量 | 宇宙 | 聚合 | 用途 |
|---|---|---|---|---|---|
| D01 | 总人口 | B01003_001 | 总人口 | sum | 密度、覆盖人口 |
| D02 | 户数 | B25003_001（已入住住房单元） | 已入住住房单元 | sum | 户数类比例的分母 |
| D03 | 住房单元 | B25001_001 | 住房单元 | sum | 职住比副口径分母 |
| D04 | 18 岁以下人口 | B01001_003–006 + 027–030 | 总人口 | sum | 公平：年龄结构 |
| D05 | 65 岁以上人口 | B01001_020–025 + 044–049 | 总人口 | sum | 公平：年龄结构 |
| D06 | 16 岁以上工作者 | B08301_001 | 16 岁以上工作者 | sum | 通勤类比例的分母 |
| D07 | 通勤方式计数 | B08301_002 小汽车合计、003 独自驾车、004 合乘、010 公共交通合计（011 公交、012 地铁或高架、013 通勤铁路、014 轻轨或有轨电车、015 渡轮）、016 出租车或网约车、017 摩托车、018 自行车、019 步行、020 其他、021 在家办公 | 16 岁以上工作者 | sum | 方式选择参考、在家办公比例；代码已按原始 JSON 逐字核对，共 21 个变量 |
| D08 | 出发上班时段计数 | B08302_002–015，共 14 档 | 不在家工作的 16 岁以上工作者 | sum | 峰值小时份额的推算来源 |
| D09 | 零车户 | B25044_003（自有住房、无车）+ B25044_010（租赁住房、无车） | 已入住住房单元 | sum | 公平；分母 B25044_001。`_002` 与 `_008` 分别是自有住房合计与自有住房五辆以上，不可用 |
| D10 | 家庭收入档户数 | B19001_002–017，16 档 | 户 | sum | 低收入户比例、分组中位数 |
| D11 | 家庭收入中位数 | B19013_001 | 户 | not_aggregatable | 仅逐 block group 展示 |
| D12 | 贫困线以下户数 | B17017_002 | 户 | sum | 公平；分母 B17017_001 |
| D13 | 残障人口 | B18101 各"有残障"单元（004、007、010、013、016、019、023、026、029、032、035、038） | 平民非机构人口 | sum，**tract 级** | 公平；分母 B18101_001。2026-09-15 实测：该表在 block group 级不发布，API 对全部 1,843 个 block group 返回 null；改为拉取 tract 级，按 tract 陆地份额分摊，粒度比其他指标粗，字段标 `geography_unit: tract` |

派生比例（环带层计算，`moe_rule: ratio_formula`）：

| ID | 指标 | 分子 ÷ 分母 |
|---|---|---|
| R01 | 公共交通通勤份额（参考） | B08301_010 ÷ B08301_001 |
| R02 | 在家办公比例 | B08301_021 ÷ B08301_001 |
| R03 | 零车户比例 | D09 ÷ B25044_001 |
| R04 | 低收入户比例 | D10 中年收入低于 35,000 美元的六档（002–007）÷ B19001_001；阈值是本项目惯例，不是官方低收入定义 |
| R05 | 贫困户比例 | D12 ÷ B17017_001 |
| R06 | 残障人口比例 | D13 ÷ B18101_001 |
| R07 | 65 岁以上比例 | D05 ÷ D01 |
| R08 | 环带家庭收入中位数估计 | 由 D10 汇总后分组内线性插值；落入 200,000 美元以上开放档时只报下界；给 `sensitivity_range`，取所在档的上下边界 |
| R09 | 人口密度 | D01 ÷ 环带陆地面积，展示为人／sq mi |

## 4. 就业层指标（LODES 8，2023，2020 block）

| ID | 指标 | 文件 / 列 | 聚合 | 说明 |
|---|---|---|---|---|
| J01 | 岗位数 | WAC `C000`，JT00 全部岗位与 JT01 主要岗位各一 | sum | `C000` 官方定义是"岗位总数"；JT01 每人一份主要岗位，可近似在此工作的人数 |
| J02 | 岗位按收入档 | WAC `CE01`（≤ 1,250 美元／月）、`CE02`（1,251–3,333）、`CE03`（> 3,333） | sum | 商业可行与公平 |
| J03 | 岗位按行业 | WAC `CNS01`–`CNS20` | sum | 展示时可分组，原始 20 类保留 |
| J04 | 居民持有岗位数 | RAC `C000`，JT00 全部岗位与 JT01 主要岗位各一 | sum | 官方定义同样是"岗位总数"，按居住地统计；只有 JT01 可近似"居住工人数"，JT00 不得如此命名。职住比主口径分母用 JT01 |
| J05 | 居住工人按收入档 | RAC `CE01`–`CE03` | sum | 公平 |
| J06 | OD 子集 | OD main + aux，`h_geocode`、`w_geocode`、`S000`，JT00 与 JT01 | 行级保留 | 只保留至少一端落在研究 block 集合内的行；每行附 `h_candidate_stations`、`w_candidate_stations`（站点 ID 与落带份额）、`link_category`（both_in / home_in_work_elsewhere_in_state / work_in_home_elsewhere_in_state / work_in_home_out_of_state，最后一类只来自 aux）。居住地在内、工作地在州外的联系不在佛州文件中，记为已声明缺口，不计数 |
| J07 | OD 类别总量 | 由 J06 汇总 | sum | 含窗口外一端的总量，不删 |
| J08 | 岗位密度 | J01 ÷ 环带陆地面积 | ratio | 展示为岗位／acre |
| J09 | 职住比主口径 | WAC `C000` JT01 ÷ RAC `C000` JT01，均 2023 | ratio | 同年同就业类型；JT00 版本另报 |
| J10 | 职住比副口径 | WAC `C000` JT01（2023）÷ D03 住房单元（ACS 2020–2024） | ratio | `mixed_period: true` |

LODES 全部标 `data_nature: administrative_modeled`、`moe_status: not_applicable`、`is_ridership: false`、`has_time_of_day: false`。文件中不存在的 block 视为零岗位，`value_status: zero_absent_from_file`。噪声保护与覆盖范围说明从技术文档 8.4 抄录进清单。

## 5. POI 层与步行路网模型层

| ID | 指标 | 定义 | 单位 |
|---|---|---|---|
| P01–P04 | 环带内设施数 | 学校、诊所、杂货店、公园四类；类别定义与来源字段映射在该层清单中固定。**已于 2026-09-17 实现**（阶段 `poi`）：学校 = 县 MD_Emaps 公立（在校生数 > 0；教育局办公楼、车库、通信中心、成人技术学院与零在校生的项目点排除；有在校生的虚拟学校保留，因来源未标注）+ 特许 + 私立；公园 = 市 `Park_Boundary_Base_Layer` 多边形 + 与之不重叠的县公园边界；诊所 = 县五个公共健康设施层去重；杂货店 = USDA SNAP 零售商中 Supermarket / Super Store / Grocery Store，OSM 只作交叉核对。点设施按落在环带内计数，公园多边形按与环带相交计数 | 个 |
| P05 | 最近设施距离 | 每类一项，`distance_type: straight_line`（首版）或 `network_walk`；起点为 GTFS 参考点，只在研究窗口内搜索 | ft |
| P06 | 缺失状态 | `absence_status: not_found_in_source_within_window` / `confirmed_absent`（后者需核实记录，目前未用） | |
| W01 | 15 分钟步行等时圈面积 | OSM 路网，步速为假设参数，起点为 GTFS 参考点 | acre、sq mi |
| W02 | 等时圈内人口与岗位 | 用第 6 节同一分摊方法 | 人、岗位 |

W 层全部标 `catchment_type: network_walk_model`、`is_validated_walk_catchment: false`，并记录 OSM 抽取日期、路网库版本、步速假设与入口假设；验证清单：入口、合法过街、垂直连接、通行权限、关键断点。

## 6. 分摊与汇总规则

- 环带集合：每站 b1、b2、b3 三个环带，`cum` 累计盘，`net` 全网并集；`cum` 与 `net` 直接从来源单元计算，不从环带结果合并。
- 陆地几何：用同版本 TIGER/Line 县级水域面要素 AREAWATER 从 block 完整多边形中擦除水域，得到每个 block 的陆地多边形。这是分子与分母共用的唯一陆地口径，方法标 `land_geometry_method: tiger_areawater_erased`。擦除后各 block 陆地面积与 `ALAND` 交叉核对并记录偏差；陆地多边形为空的 block 份额为 0。
- 来源单元份额：block 的份额 = 该 block 陆地多边形与目标几何的交集面积 ÷ 该 block 完整陆地多边形面积；block group 的份额 = Σ(其各 block 陆地多边形与目标几何的交集面积) ÷ Σ(其各 block 陆地多边形面积)。分母是完整陆地几何，不被研究窗口裁切。环带只碰到某 block 的水域部分时，该 block 份额为 0。
- 环带陆地面积 = Σ(各 block 陆地多边形与环带的交集面积)，用于密度分母。
- "block 总几何相交比例 × ALAND"假设陆地在 block 内均匀分布，只作为对照诊断输出，命名为 `block_aland_ratio_approximation`，不用于正式结果。
- 计数类：`估计 = Σ(wᵢ × 估计ᵢ)`，`moe_90 = √Σ(wᵢ × moeᵢ)²`，`moe_status: approximate_sampling_only_excludes_interpolation`。
- 比例类：分子分母分别按上式汇总后再相除；MOE 用 Census 比例近似公式，`moe_status: ratio_approximation`。
- 中位数：不汇总；环带级用 R08 的分组插值。
- 户数类指标默认 `weight_proxy: land_area`；改用住房或家庭分布作权重时另行注明。2020 普查 block 计数权重列为后续选项，本阶段不用。
- 每站画像非排他；`net` 层另报"各站相加 ÷ 去重"的重复比与被两站以上覆盖的人口、岗位。

## 7. 缺失状态映射

| 来源标记 | 契约状态 | 说明 |
|---|---|---|
| ACS 估计值 `-666666666` | `missing`，`missing_reason: insufficient_sample` | 样本不足，未发布估计 |
| ACS 估计值 `-999999999` | `suppressed`，`missing_reason: not_published` | 估计或 MOE 不可用 |
| ACS 估计值 `-888888888` | `not_applicable` | 不适用 |
| ACS MOE `-222222222` | `moe_status: insufficient_sample` | 样本不足无法计算 MOE |
| ACS MOE `-333333333` | `moe_status: open_ended_interval` | 中位数落在开放档 |
| ACS MOE `-555555555` | `moe_status: exact_source` | 受控估计，无 MOE |
| ACS 估计值 0 | `value`，value 0 | 估计为零，仍带 MOE |
| LODES 文件中无该 block | `zero_absent_from_file` | 视为零岗位 |
| POI 来源中未检索到 | `absence_status: not_found_in_source` | 不等于不存在 |
| POI 经核实不存在 | `absence_status: confirmed_absent` | 需要核实记录 |
| Miami 特定输入未取得 | `acquisition_status: not_acquired`；同时 `value_status: missing`、`missing_reason: not_acquired` | 获取状态与数值状态是两个字段，见第 8 节 |

ACS 特殊值以 API 注释文档为准，下载时核对一次并记入清单。

## 8. Miami 特定输入（保留字段；`acquisition_status` 是输入级的获取状态）

`acquisition_status` 枚举：`acquired` / `derived`（由已取得的数据推算得到，如 GTFS 时刻表推算的服务基线）/ `assumed`（未取得，用演示假设代替，此时值的 `provenance_class` 为 `assumption`）/ `not_acquired` / `derivable_from_gtfs` / `not_approved`。它描述数据是否取得，与数值级的 `value_status` 分开；未取得时 `value` 为 null、`value_status: missing`、`missing_reason: not_acquired`。

| 字段 | 内容 | `acquisition_status` |
|---|---|---|
| `ridership_baseline` | Metromover 分站上车量，来源 Miami-Dade DTPW 月度《Ridership Technical Report》的"Metromover Monthly and Average Daily Boardings by Station"表，2025-10 至 2026-07 共 10 个月 | `acquired`（2026-09-17，`data_nature: measured`）：分站平均工作日、周六、周日上车量与月总量，10 个月均值与最新月。**只是上车量，不是出行量、OD、下车量或分时流量，只作参照、不作校准**；三个改名站（Omni / Park West / Eighth Street）的对应待业主确认 |
| `service_baseline` | 各时段班距，可由 GTFS `stop_times` 推算 | `derivable_from_gtfs` |
| `hub_transfer_inflow` | Metrorail、Brightline 等换乘入口与换乘量 | `not_acquired`，Miami 不建模 |
| `road_guideway_roles` | 道路与轨道的功能、层位、模式、货车限制 | `not_acquired` |
| `platform_module_specification` | 站台模块图纸（2026-09-17 由业主提供）：M1 双侧斜列 6 泊位 50'3" × 77'-10"、3,908 ft²；M2 单侧斜列 7 泊位 32'-3" × 132'-7"、4,271 ft²。以英制为准，公制图原值另存，二者相差不超过 0.6% | `acquired`，`data_nature: design_specification` |
| `platform_constructible_space` | 每站可建设空间 | `assumed`（C06：假定所选模块占地每站可建；站点可建设空间仍未取得） |
| `vehicle_parameters` | 车辆参数 | `assumed`：车辆能完全停入泊位为业主陈述，几何尺寸不再需要；运营参数（平均同行人数、泊位周期、利用率、小时内峰值系数、最小行车间隔、无暂存泊位）为通用 PRT 假设，见 `config/prt_operations_assumptions_v1.json` |

## 9. 情景结果字段（分析层产出，不入输入包）

每个情景结果带：`scenario_id`（low / medium / high）、`chain`（计算链各步的输入 ID、参数 ID 与数值）、每个参数的三组标记、`conservation_check`（分配前后总量守恒）、`unassigned_share`（未使用本系统余量）、`excludes: ["hub_transfer_inflow"]`。上车与下车分开给出；返程标 assumption。

已于 2026-09-15 实现（第 5 步，`scripts/build_miami_scenarios.py`，阶段 `scenarios`）：基数只取两端都在全网 ½ mile 并集内的 LODES 主要岗位联系，且按两端街区陆地落在并集内的份额计入（2026-09-17 修正：只碰到圆的街区不整块纳入，整块口径另报为上限）；链为 联系 → 工作日通勤出行（ACS 在家办公比例、到岗率假设）→ 全目的出行（非通勤倍数假设）→ 高峰小时（ACS 出发时段推算的单一份额）→ PRT 出行（采用率假设，低档不超过观测公交分担率）→ 分站上下客（按候选站点份额分配，守恒）；PM 为 AM 镜像。三档只改 A02 与 A03。结果 `data_nature: model_output`、未校准（客流基线未取得），排除一端在网外的联系与枢纽流入。服务基线同日由 GTFS 推算（阶段 `service_baseline`），进入输入包后 `acquisition_status: derived`。

## 10. 就绪标志

| 字段 | 类型 | 判定 |
|---|---|---|
| `ready_for_ai_interpretation` | 布尔 | 站点主表、空间连接、人口层、就业层的包均 DONE 且版本已锁定 |
| `ai_missing_optional` | 列表 | 缺失的可选层（POI、步行模型），不影响上一字段 |
| `config_inputs_complete` | 布尔 | 配置计算所需的每个输入都有值，不论观测还是假设：情景结果、站台可建设空间、车辆参数、服务基线 |
| `config_inputs_all_observed` | 布尔 | 上述输入没有任何一项由假设支撑 |
| `config_missing_inputs` | 列表 | 缺失的输入 ID |
| `config_assumption_backed_inputs` | 列表 | 由假设支撑的输入 ID |

"可计算"即 `config_inputs_complete` 为真；"使用假设计算"即 `config_inputs_complete` 为真且 `config_inputs_all_observed` 为假。两者独立判定，都是布尔值，不混用字符串。这些字段由 `scripts/miami_readiness.py` 计算写入 readiness 的 `readiness_flags` 段，替代单一 DONE 的含义；各层自身仍用 DONE / STALE / CORRUPT_PACKAGE / NOT_RUN，列在 `stages` 段。

聚合器的读取位置：输入包 manifest 的 `config_inputs` 摘要（每个配置输入的 `acquisition_status`、`value_status`、`provenance_class`）与情景包 manifest 的 `assumption_parameter_ids`。"有值"= `value_status: value`；"假设支撑"= `provenance_class: assumption` 或 `acquisition_status: assumed`。已于 2026-09-15 实现（第 1 步）；各层落地前六个字段显示 false 并列出缺项，这是正确输出而不是错误。输入包阶段（第 4 步，`scripts/build_miami_input_package.py`）同日实现：它按当前指针锁定空间连接、人口、就业三层的包 ID，逐站复制各层的数值信封并加 `evidence_source` 指向，不重算任何数值；manifest 的 `config_inputs` 摘要当时三项都未取得，因此 `ready_for_ai_interpretation` 为 true 而 `config_inputs_complete` 为 false。2026-09-17 第 6 步后：服务基线为 `derived`，站台空间与车辆参数为 `assumed`，配置阶段（`scripts/build_miami_config.py`，阶段 `config`）已实现，就绪状态为"完整为真、全部观测为假"，假设支撑项为站台空间、车辆参数与情景结果。

## 11. 依赖登记

布局约定：每个包阶段发布到 `data/miami/2026-09-14/<stage>/pkg-<id>/`，包内 `manifest.json`（空间连接阶段沿用旧名 `spatial_join_manifest.json`），指针文件 `<stage>_current.json` 记录 `package_id`、`package_dir`、`manifest_sha256`。阶段登记表与构建顺序在 `scripts/miami_readiness.py` 里：station_master、spatial_join、demography、jobs、poi、walk_model、input_package、scenarios。

每个阶段的 manifest 登记：`consumed_inputs`（`name`、ROOT 相对路径、sha256）、`consumed_packages`（阶段名、包 ID；只引用包阶段，站点主表通过其文件列入 `consumed_inputs`）、`code`（脚本路径与 sha256）、`config_sha256`、`environment`、`ready_for_downstream`。聚合器遍历这些登记项判定过期，不再只检查空间连接的三个输入：

- 任一 `consumed_inputs` 文件哈希变化，或任一 `consumed_packages` 不再是该阶段的当前包，即 STALE；被消费的阶段本身不是 DONE 时，本阶段也 STALE（过期沿构建顺序向下传递）。
- 代码变化不算过期：包记录了生成它的脚本哈希；当前脚本不同时 `code_matches_current` 为 false，提示重建，但已发布结果与其记录的输入仍然一致。
- 站点主表阶段无包，按 `build_manifest.json` 判定：产物哈希不符即 CORRUPT_PACKAGE，记录的数据输入变化即 STALE，脚本变化只影响 `code_matches_current`。
- 空间连接现有包的 manifest 仍是 2.0 格式（`upstream` 块），聚合器按兼容映射读取；下次重发包时补齐通用字段。

最终输入包记录所用各层的包 ID；任一层发布新包后，输入包标 STALE，直到重新汇总。
