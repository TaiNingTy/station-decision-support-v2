# V2 数据语义与客流转换规则

第三版，2026-09-15。第一版经独立评测提出七项修订，第二版全部并入并记录四项已定决策；第二版再经评测发现字段契约中的变量映射错误、陆地分摊算法与规则表述不一致、LODES aux 覆盖表述不准确及两处类型不一致，本版同步修正。适用于 Miami 先行，Melbourne 与 Singapore 沿用。这些规则约束数据层的字段编码和分析层的用法；字段级定义见 [V2 站点输入包字段契约](V2_站点输入包字段契约.md)。面试时每个数字都要能追溯到规则、来源与假设。

## 已定决策（2026-09-15）

| 决策 | 内容 |
|---|---|
| 情景基数 | 取全方式通勤出行。PRT 采用率定义为占全方式出行的份额；现状公交分担率只作低档情景的参考上限，不进入乘法链 |
| 枢纽流入 | Miami 保留字段但不建模，情景表写明"未含枢纽换乘流入"。Miami 案例展示全线替换；transit hub 流入是 Melbourne 案例的重点。**成果含义限制**：可展示全线站点的配置工作流，不能据此证明全部现有乘客需求下的全线替换容量足够 |
| 职住比 | 主口径 = 岗位 ÷ 就业居民，用同年、同就业类型的 WAC 与 RAC，具体取 JT01 主要岗位（RAC 的 JT01 才可近似居住工人数）；副口径 = 岗位 ÷ 住房单元，注明跨统计期 |
| 就绪状态 | 拆为独立布尔字段：`ready_for_ai_interpretation`；配置计算用 `config_inputs_complete` 与 `config_inputs_all_observed` 两个布尔值分别表示"可计算"与"是否全部有观测依据"，各带缺失与假设清单；由聚合器按各层状态计算，不再用单一 DONE，也不混用字符串 |

## 总原则

- 输入包只放事实、来源和假设标记，不放结论。需求、采纳、商业可行、公平四类分析各自读取同一份输入包，结果不回写进包里。
- AI 与确定性规则计算读同一份包。AI 的引用分三类：场地事实引 `evidence_id`；规范判断引 `rule_id` 与知识库文档版本；方案比较引计算结果 ID。仅引用输入包里的数据证据不能支撑全部工程判断。
- AI 不自行编造来源描述；网页仍提供可读的来源摘要。
- 每个数值带三组标记：`provenance_class`（observed 观测、derived 推算、assumption 假设）；`data_nature`（measured 实测、survey_estimate 调查估计、administrative_record 行政记录、administrative_modeled 行政数据建模、model_output 模型输出、assumption 假设）；`value_status`（value 有值、missing 缺失、suppressed 被抑制、not_applicable 不适用、confirmed_zero 确认为零、zero_absent_from_file 文件中缺省视为零）。`observed` 不等于实测，LODES 就是行政数据建模。
- `derived` 可以依赖假设；所依赖的假设列在 `depends_on`，假设变化时派生结果必须重算并继承其不确定性标记。
- 每一层沿用空间连接阶段的机制：整包发布、内容寻址包目录、指针原子切换。每个阶段的 manifest 登记自己消费的全部数据、包、配置与代码依赖；聚合器遍历这些登记项判定 DONE / STALE / CORRUPT_PACKAGE，不再写死三项。最终输入包锁定各层的具体包 ID，不读取"当前指针"。

## 规则 1 · ACS 5 年期是期间估计，不是当前人口

ACS 是统计估计。5 年期数据汇集了五年的调查，不能标成当前或实时人口，也不是某个时点的快照；必须保留统计期和误差。

编码：

- 每个数值带 `period`（2020–2024）、`vintage`（ACS 2024 5-year）、`moe_90`、`estimate_type: period_estimate`、`is_current_population: false`。
- MOE 按三种情形分别处理，不套同一个公式：
  - 纯加总（同一来源单元内的分项相加）：各项 MOE 平方和开方。
  - 按份额分摊到环带：把分配权重视为固定，`环带估计 = Σ(wᵢ × 估计ᵢ)`，`近似 MOE = √Σ(wᵢ × MOEᵢ)²`。这个 MOE 只含抽样误差，不含空间分配本身的不确定性，字段标 `moe_status: approximate_sampling_only_excludes_interpolation`。
  - 占比类指标：先分别汇总分子与分母，再算比例；比例的 MOE 用 Census 的比例近似公式。占比本身不可直接加总。
- 累计盘与全网结果一律回到来源单元重算，不从环带结果二次合并；同一个 block group 拆到多个环带后，各环带的误差不独立。
- Census 的方差复制表能给含协方差项的精确 MOE，但只覆盖部分表格与地理；演示阶段不用，写明"近似公式"。
- 变异系数按契约中的阈值加 `reliability_flag`，网页显示"仅供参考"。
- 地理单元：ACS 用 block group，LODES 用 2020 普查 block。

## 规则 2 · LODES 是职住联系，不是客流

Census 官方说明：LODES 描述居住地、工作地及两者之间的就业联系；数据基于行政数据的汇总与建模，不适用抽样误差指标。它可以帮助建立职住关系，但不能直接变成高峰小时乘客量。

编码：

- 数据层只保存 block 级产物：各 block 的岗位数（WAC）、居住工人数（RAC），以及 block 到 block 的 OD 子集。**不输出站对站矩阵。** 同一条就业联系两端各被两站覆盖时，站对站矩阵会把它记成四份，虚增四倍。
- OD 子集的两端各附候选站点列表与落带份额，只是关联，不是分配。站对站分配、总量守恒检查和"未使用本系统"的余量放分析层。
- 研究窗口外的起终点不静默删除：按"两端在内 / 仅居住地在内 / 仅工作地在内"分类计数保留，避免漏掉经区域枢纽进入的通勤联系。
- 州内 main 文件与 aux 文件一起取。技术文档 8.4 原文定义：main 含居住地与工作地都在本州的岗位，aux 含工作地在本州、居住地在州外的岗位。佛州居民在州外工作的联系在其他州的 aux 文件里，本项目不取，作为已声明缺口。就业覆盖范围以技术文档为准，下载时抄录进清单。
- `C000` 在 WAC 与 RAC 里的官方定义都是"岗位总数"。RAC 的 JT00 是居民持有的岗位数，不得命名为居住工人数；只有 JT01 主要岗位每人一份，可近似居住工人数。
- 字段标 `year: 2023`、`lodes_version: 8`、`job_type`（JT00 全部岗位、JT01 主要岗位）、`is_ridership: false`、`has_time_of_day: false`、`data_nature: administrative_modeled`、`moe_status: not_applicable`。
- 职住比按已定决策：主口径 WAC ÷ RAC，同年同 `job_type`；副口径岗位 ÷ 住房单元，标 `mixed_period: true`。

## 规则 3 · 站间重叠：分站展示非排他，全网去重

站点之间的重叠必须处理。每站画像可以分别展示；计算全网覆盖人口时需要去重。

编码：

- 每站画像保持非排他，各自展示，沿用 zoning 阶段的 `is_allocation_weight: false`。
- 全网覆盖用现有的全网并集几何，从来源单元一次性计算，不用各站相加；同时报"相加值 ÷ 去重值"的重复比，以及被两站以上覆盖的人口与岗位。
- 重叠区域归哪一站是分配假设，不在数据层决定；数据层只给候选站点列表。

## 规则 4 · 中位数不能加权平均，权重按指标选

收入中位数不能简单按面积加权平均，也不能按人口加权平均。分摊权重要按指标性质选。

编码：

- 不对 block group 中位数做任何平均。需要环带级中位数时，用收入分组表 B19001 把各档户数按落带份额加总，再做分组插值，标 `estimated_from_aggregated_brackets`。中位数落入最高开放档时只报下界。误差用敏感性区间，字段名与置信区间明确区分：`sensitivity_range` 不是 `moe_90`。
- 各 block group 的原始中位数及其 MOE 保留，供逐单元查看。
- 落带份额默认按陆地几何分摊：用同版本 TIGER/Line 县级水域面要素从 block 完整多边形中擦除水域，分子与分母都取擦除后的陆地多边形，分母是来源单元完整的陆地多边形，不被研究窗口裁切；环带只碰到某 block 的水域部分时该 block 份额为 0。"block 总几何相交比例 × ALAND"假设陆地在 block 内均匀分布，只是按陆地面积校正的近似分配，仅作对照诊断，不用于正式结果。Miami 水域太多，用总面积会系统性低估临湾站点。
- 户数类指标（收入档户数、零车户、贫困户）不自动套人口权重；默认同样用陆地面积并标 `weight_proxy: land_area`，若改用住房或家庭分布作权重则另行注明。
- 2020 年普查 block 人口与住房计数可作为空间分布代理改善分摊，但它与 ACS 有年份差异，且属于新增数据源；本阶段不加，列为后续选项。
- 公平分析优先用可加总的分子与分母：低收入户数与户数、零车户数与户数、贫困户数与户数、老年与残障人口数与总人口，各自带 MOE，比例在汇总后计算。

## 规则 5 · 密度与可达性地图不是客流热力图

> 人口密度、岗位密度和 POI 可达性可以形成地图，但还不能直接作为"人／小时"的客流热力图。要转换成站点峰值上下客量，还需要时段分布、出行目的、方式选择、PRT 采用率及站点分配假设。我们可以为面试准备低／中／高情景，并明确哪些参数有观测依据、哪些只是演示假设。

编码：

- 密度与可达性图层标 `is_ridership: false`。密度用人／sq mi、岗位／acre、户／acre；距离用 ft 与 mi；时间用分钟；不出现"人／小时"。
- 站点峰值上下客量只在情景结果里出现，每个结果带情景名、参数链和各参数的三组标记，网页上点任一数字可见参数链。
- 计算链固定为：**就业联系或其他活动基础 → 指定日型的出行人次 → 时段分布 → 服务选择与站点分配 → 分站上车、下车人次。** 每一步的输入输出在契约里定义。
- 峰值参数只有一个：`peak_hour_share`。它的来源二选一，ACS B08302 离家时段分布（推算）或 Metromover 分时客流报表（观测）；禁止把"时段分布"与"峰值系数"连乘。
- B08302 的宇宙是"不在家工作的 16 岁以上工作者"，字段是"出发上班时间"，不是抵达站点时间，也没有返程分布。返程按早高峰镜像加一个工时假设，标 assumption。
- 方式选择与采用率的条件关系：基数是全方式通勤出行，采用率是占全方式出行的份额；不得把现状公交分担率再乘到已经限定为公交乘客的基数上。
- NHTS 全国或区域比例用于 Miami 时，本地迁移系数标 assumption；原始调查真实不等于迁移结果是本地观测。
- 低／中／高三档只改 assumption 类参数，observed 类参数三档共用；情景表随结果一起发布。

| 参数 | 作用 | 来源 | 状态 |
|---|---|---|---|
| 日出行基础 | 就业联系转成指定日型的出行人次：到岗率、远程办公、方向、去程与返程 | ACS B08301 在家办公比例（观测）；到岗率与返程规则（假设） | 部分有观测 |
| 非通勤出行 | 通勤之外的出行占比 | NHTS 比例，本地迁移系数为假设 | 借用观测，需标注 |
| 峰值小时份额 | 日量转到高峰小时 | ACS B08302（推算）或 Metromover 分时客流（观测），二选一 | 待取得报表 |
| 方式选择参考 | 现状通勤方式，只作参考上限 | ACS B08301（观测） | 有观测，不入链 |
| PRT 采用率 | 占全方式出行的份额 | 无观测 | 演示假设，低／中／高 |
| 站点分配 | 重叠区域的出行归哪站，须总量守恒并保留未使用余量 | 无观测 | 演示假设，规则显式 |
| 枢纽流入 | 经 Metrorail、Brightline 等进入的换乘量 | 未取得 | 保留字段，Miami 不建模 |

实现状态（2026-09-15，第 5 步）：日出行基础用 ACS 在家办公比例（推算，27.3%）与到岗率 0.85（假设）；峰值小时份额由 B08302 推算为 8:00–8:59 占非在家办公工作者的 35.5%（推算，依赖"离家时段≈抵站时段"假设），Metromover 分时客流未取得；方式选择参考为观测公交分担率 6.1%，只作低档采用率上限；采用率三档 0.05 / 0.15 / 0.30，非通勤倍数三档 0.5 / 1.0 / 1.5，均为演示假设；站点分配按候选站点份额归一，守恒；枢纽流入未建模。基数只含两端都在网内的主要岗位联系，并按两端街区落在全网并集内的陆地份额计入（8,815；整块纳入的上限为 9,706），因此三档早高峰网内出行量 145 / 580 / 1,449 人次是不含区域流入的下限量级，不是需求预测。

## 规则 6 · 步行范围与 POI 可达性是模型估计

- 用参考点、OSM 路网和步速算出的等时圈标 `catchment_type: network_walk_model`、`is_validated_walk_catchment: false`。确认入口、合法过街、垂直连接、通行权限和关键断点之后，才提升验证状态。Miami 高架站附近有一个 GTFS 点，不足以证明乘客能进入站台。
- POI 的最近距离注明 `distance_type: straight_line` 或 `network_walk`；`absence_status` 区分"来源中未检索到"与"确认不存在"。
- POI 只给数量与距离，不打"服务水平"分。

实现状态（2026-09-17，第 7b 步）：POI 层已按此规则接入（阶段 `poi`）。来源为县、市与 USDA 的官方开放数据，OSM 只作杂货店交叉核对；类别规则固定在包内 `config.rules`（学校取公立在校生数 > 0 的记录加特许与私立，公园取市多边形加不重叠的县多边形，诊所五层去重，杂货店取 SNAP 三种店型）；距离一律 `straight_line`，起点为 GTFS 参考点；缺失为 `not_found_in_source_within_window`，"确认不存在"未使用，因为没有做过实地或第二来源核实。被排除的记录连同原因保留在 `poi_features.json → excluded_records`。

## 规则 6b · 实测客流只作参照，不作校准（2026-09-17）

DTPW 月度报表给出现有 Metromover 分站的日均上车量（10 个月均值，系统合计约 25,400 人次/工作日）。它是观测值，但描述的是现有系统的进站人数，不是新系统的需求，也没有 OD、下车量和分时分布。用法只有两种：一是量级参照，把情景层的日出行量与之相除，说明情景只覆盖网内联系（低／中／高约为观测的 3% / 13% / 32%）；二是分布参照，比较各站份额的形状（秩相关约 0.20），差异最大的是 Government Center，观测占 20% 而情景只占 3%，原因正是情景排除了 Metrorail 换乘等区域流入。任何按观测值缩放情景的做法都是校准，需要先取得 OD 与分时数据并明确标注，本项目未做。

## 规则 7 · PRT 语义：人次到泊位的换算（2026-09-17）

系统是 PRT，每车最多 4–6 人。情景层给出的"人次"不受车型影响，但供给侧必须按 PRT 换算，不得沿用大运量的习惯用法；V1.1 知识库里"每泊位每小时若干人"的约定作废，全部由参数推导。

- 人次 → 车次：上车人次 ÷ 平均同行人数（C01，假设，通勤多为单人）。车辆最大载客只用于校验，绝不作为载客量。
- 车次 → 泊位周期：每个泊位周期含一次进站与一次出站（载客或空车），每小时周期数 = max(到达车次, 出发车次)。
- 泊位数：周期数 × 小时内峰值系数（C04，只作用于泊位计算，绝不再乘回需求链）÷ (3600 ÷ 泊位周期 C02 × 利用率 C03)，向上取整。
- 模块与占地：取业主图纸中泊位数足够的最小模块（M1 双侧 6 泊位、M2 单侧 7 泊位），占地按图纸；超过最大模块时只标记不设计。
- 空车平衡：出发多于到达的站，差额即每小时需调入的空车（图纸无暂存泊位，C07）。
- 轨道流量：全网车次 ÷ (3600 ÷ 最小行车间隔 C05)，只是网络级比值；哪条轨道承担哪些车次需要线路模型，本项目没有。
- 站点可建设空间未取得：以 C06 假定所选模块每站可建，就绪状态如实标为"假设支撑"。
- 结果是模型输出，不是设计，也不能证明全线替换的容量足够（需求侧已排除区域流入与枢纽换乘）。

## 搭建顺序

0. **字段契约**：先定指标、分子分母、年份、地理版本、单位与缺失状态，再写各层。见字段契约文档。
1. **人口与就业层**：ACS 5 年期 block group 与 LODES block 落到每站三个环带、累计盘和全网去重范围；LODES 只出 block 级产物与带候选站点的 OD 子集；逐单元证据行、校验与 manifest，整包发布。
2. **POI 层**：学校、诊所、杂货店、公园四类，记录来源与抓取时间，给数量与最近距离。（已完成，2026-09-17）
3. **步行路网模型层**：OSM 路网加 15 分钟步行等时圈，作为模型估计与欧氏环带并列，不替代。（未做；是唯一仍缺的可选层）
4. **站点输入包**：按各层包 ID 汇总为每站一份输入包，锁定版本，字段级带来源、统计期、误差与假设标记；聚合器给出两个就绪标志。其后才是分析层、规则层按 V2 语义重写、Coze 批跑留痕、静态网页。

数据获取走可复算路径：Census API 密钥只存本地不进仓库，或直接下载官方文件并记录 sha256；LODES 与 TIGER 均为固定文件下载。本阶段不增加统计数据源；aux 文件属于同一份 LODES 下载，另取 LEHD 的官方 sha256sum 文件核对；TIGER 水域面要素与 block 多边形属于同一 TIGER/Line 产品。
